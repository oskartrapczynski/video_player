import { Register } from '@/components';
import { REGISTER_INPUT } from '@/constants/index';

const UserRegisterPage = () => {
  return <Register inputForm={REGISTER_INPUT} />;
};

export default UserRegisterPage;
