export default interface Borrow {
  title: string;
  genre: string[];
  director: string;
  length: string;
  rate: number;
  description: string;
  actors: string[];
  releasedAt: number;
  addedAt: number;
}
